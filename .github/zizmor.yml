# Mirrors ai/shared/skills/github-actions/references/structure.md: trusted
# publishers pin to a major tag, everything else to a full commit SHA.
rules:
  unpinned-uses:
    config:
      policies:
        actions/*: ref-pin
        github/*: ref-pin
        "*": hash-pin
