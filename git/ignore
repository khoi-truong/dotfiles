# ~/.config/git/ignore — linked from ~/.dotfiles/git/ignore by git/setup.sh.
# Global ignores for every repo; project-specific ones belong in .gitignore.

# macOS
.DS_Store

# Claude Code `--worktree` checkouts
.claude/worktrees/

# herdr team, this machine's project-local settings
.config/herdr/team.local.toml
