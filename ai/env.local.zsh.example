# Copy to ai/env.local.zsh (gitignored) and fill in.
# Sourced by ai/aliases.zsh, which zshrc sources.
#
# Claude Code normally authenticates through the subscription login, so this is
# only needed for direct API usage.
# export ANTHROPIC_API_KEY="sk-ant-..."
